.env
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# topics.queue.tsv and topics.queue.example.tsv are committed (the
# scheduled workflow reads the queue from the repo). Scratch / backup
# queues belong in your personal .gitignore_local, not here.

# Logs
logs/*.log
logs/*.log.*

# OS junk
.DS_Store
Thumbs.db
